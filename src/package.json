{
  "name": "scribae",
  "version": "1.3.0",
  "private": true,
  "description": "Scribae — éditeur de trames et d'actes administratifs. Racine du dépôt : la vérification de syntaxe et les tests qui s'exécutent sans navigateur se lancent d'ici.",
  "license": "GPL-3.0-only",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "node scripts/verifier-syntaxe.mjs && node scripts/verifier-style.mjs",
    "syntaxe": "node scripts/verifier-syntaxe.mjs",
    "style": "node scripts/verifier-style.mjs --strict",
    "test": "node --test tests/ server/mysql/",
    "verifier": "npm run lint && npm test"
  }
}
